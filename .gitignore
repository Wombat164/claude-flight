# Never publish a real deployment's config (may hold a capability URL).
*.conf
!*.conf.example

# Runtime state / logs.
state/
*.log
*.heartbeat
*.lock

# Quartz build scratch (framework is fetched, not vendored -- see wiki/README.md)
.quartz-build/
